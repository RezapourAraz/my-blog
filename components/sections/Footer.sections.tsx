import React from "react";

// Mui
import { Grid, Typography } from "@mui/material";

const FooterSection = () => {
  return (
    <Grid sx={{ borderTop: 2, borderColor: "grey.200", p: 2 }}>
      <Typography></Typography>
    </Grid>
  );
};

export default FooterSection;
